# Launch Screen Assets

Customize the iOS launch screen by replacing the image files in this directory.

You can also do this from Xcode:

1. Open `ios/Runner.xcworkspace`
2. Select `Runner/Assets.xcassets`
3. Drop your desired launch images into `LaunchImage.imageset`