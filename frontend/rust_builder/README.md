# Rust Builder (Glue Layer)

This folder contains the glue required to build and package the Rust library for Flutter targets.

- It is used by `rust_lib_anima` integration.
- It is not part of app business logic.
- Change files here only if you are updating the Rust/Flutter build pipeline.